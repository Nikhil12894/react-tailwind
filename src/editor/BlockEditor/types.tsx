import { Content } from "@tiptap/core";

export interface TiptapProps {
  content: Content;
  isEditable?: boolean;
  // onContentChange?: (content: Content) => void
  // onContentStateChange?: (content: Content) => void
  // onSelectionChange?: (content: Content) => void
  // onCharacterCountChange?: (content: Content) => void
  // onPaste?: (content: Content) => void
  // onDrop?: (content: Content) => void
  // onInput?: (content: Content) => void
  // onSelectionUpdate?: (content: Content) => void
  // onScroll?: (content: Content) => void
  // onMount?: (content: Content) => void
  // onUnMount?: (content: Content) => void
  // onUpdate?: (content: Content) => void
  // onResize?: (content: Content) => void
  // onResizeEnd?: (content: Content) => void
  // onResizeStart?: (content: Content) => void
  // onDropFile?: (content: Content) => void
  // onPasteFile?: (content: Content) => void
  // onPasteFiles?: (content: Content) => void
  // onDropFiles?: (content: Content) => void
  // onPasteText?: (content: Content) => void
  // onDropText?: (content: Content) => void
}
